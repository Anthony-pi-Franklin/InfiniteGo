# UI 更新总结

- 移除左侧菜单的颜色选择（进入房间时颜色锁定）
- 小地图与排行榜支持双击在菜单中嵌入/分离，菜单隐藏时嵌入组件随之隐藏
- 浮动窗口可拖拽与调整大小；嵌入状态下禁用拖拽与调整大小
- 排行榜自动高度（最多显示前 10 项），用户手动调整后暂停自动调整

实现细节参见前端模块：`minimap.js`, `leaderboard.js`, `index.html`, `styles.css`, `main.js`。
